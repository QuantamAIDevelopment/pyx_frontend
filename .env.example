# PyX AI Assistant Configuration
# Copy this file to .env and add your actual API keys

# OpenAI Configuration
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Anthropic (Claude) Configuration  
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Default AI Provider (openai, anthropic, or local)
VITE_DEFAULT_AI_PROVIDER=local

# Optional: Enable debug mode
VITE_DEBUG_MODE=false

# Local dev proxy targets for APIs (used by Vite server proxy)
# Change only if your backend runs on different ports
VITE_DEV_CONTACT_TARGET=http://localhost:8085
VITE_DEV_KEYS_TARGET=http://localhost:8084

# Optional: Absolute URLs to override in production builds
# If not set, the app will call relative paths (/api/contact, /api/keys)
# behind your reverse proxy
VITE_API_CONTACT_URL=
VITE_API_KEYS_URL=

# Optional: default userId used for creating API keys from UI
VITE_DEFAULT_USER_ID=user_11

# Note: 
# - For Vite projects, use VITE_ prefix
# - For Next.js projects, use NEXT_PUBLIC_ prefix  
# - For Create React App, use REACT_APP_ prefix
# - Get OpenAI API key from: https://platform.openai.com/api-keys
# - Get Anthropic API key from: https://console.anthropic.com/
# - The assistant will work in local mode without API keys but with limited capabilities
# - Never commit real API keys to version control

# Alternative formats for different bundlers:
# NEXT_PUBLIC_OPENAI_API_KEY=your_openai_api_key_here
# REACT_APP_OPENAI_API_KEY=your_openai_api_key_here